function showDialog(title) {
$( "#readyCheck" ).dialog('option', 'title', title);
$( "#readyCheck" ).dialog('open');
}
function hideDialog() {
$( "#readyCheck" ).dialog('close');
}
function setUpReadyCheck(){
	$( "#readyCheck" ).dialog({ 
				autoOpen: false,
			});
}
