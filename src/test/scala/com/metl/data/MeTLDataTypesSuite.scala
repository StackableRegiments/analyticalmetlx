package com.metl.data

import org.scalatest._
import org.scalatest.FunSuite
import org.scalatest.BeforeAndAfter
import org.scalatest.mock.MockitoSugar
import org.scalatest.OptionValues._

import org.mockito.Mockito._
import org.mockito.Matchers.{eq => the, any, anyInt}

import scala.xml._

class PointConverterSuite extends FunSuite {

    test("points from text") (pending)
    test("points to text") (pending)
    test("point to text") (pending)
}

class ColorConverterSuite extends FunSuite {

    test("convert from color to hex string") (pending)
    test("convert from color to rgb hex string") (pending)
    test("convert from color to argb hex string") (pending)
    test("convert from color to rgba string") (pending)
    test("convert from string to color") (pending)
    test("convert from hex string to color") (pending)
    test("convert from rgb hex string to color") (pending)
    test("convert from argb hex string to color") (pending)
}
