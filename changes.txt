1.0.5

New config elements <groupsProvider><smartGroups/></groupsProvider>.

1.0.3

METL-321:
Update config "metlingPotAdaptors", in "exclusions", to contain ApiGatewayMetlingPotAdaptor.

1.0.2

New config elements (holding credentials).

0.11.1

METL-229:
Update config "authenticationConfiguration", in "exclusions", to contain /about and /releaseNotes.

0.10.1

METL-205:
When this goes into PRD, it'll require us to redownload a cache of PRD groups in advance, so that we've got them in the new format.
We've moved from CSV to XML in our caching format, now that the data is deeper.